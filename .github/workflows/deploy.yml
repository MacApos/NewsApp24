name: GitHub Actions Demo
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.1.7
      - run: echo ${{ github.workspace }}
      
      - run: cd software/; echo ${{ github.workspace }}
      - run: echo ${{ github.workspace }}; mvn clean install --quiet; ls
        name: Create software JARs file

#      - run: cd software/Lambda/
#      - run: java -jar target/lambda.jar
#      - run: mvn clean install; ls
#        name: Run Java application
#        env:
#          CITY_API_KEY: ${{ secrets.CITY_API_KEY }}


#      - run: cdk synth; cdk bootstrap; cdk deploy --require-approval never
      # - run: cdk deploy --require-approval never
#        name: Deploy JARs to AWS services with CDK
